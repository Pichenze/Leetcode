题目 ： 给定一个只包括 '('，')'，'{'，'}'，'['，']' 的字符串，判断字符串是否有效。

有效字符串需满足：
	左括号必须用相同类型的右括号闭合。
	左括号必须以正确的顺序闭合。
注意空字符串可被认为是有效字符串。
示例 1:
输入: "()"
输出: true

示例 2:
输入: "()[]{}"
输出: true

示例 3:
输入: "(]"
输出: false

示例 4:
输入: "([)]"
输出: false

示例 5:
输入: "{[]}"
输出: true

解题思路：
考虑到括号既有左括号右括号的区分又有括号之间的匹配问题：
使用字典来存储最合适：  patterndict = {'(':')','[':']','{':'}'}  #左括号为字典的keys,右括号为字典中keys对应的values

这样可以方便直接判断当前字符是左括号还是右括号，同时在遇到需要括号匹配的情况时，可以直接通过patterndict[left] == right  #判断括号是否相符合

栈中只存放左括号，每次遇到右括号都要与栈顶的左括号进行匹配，看是否一致（如果栈为空并且读取到右括号，则为错误情况）

满足括号完全匹配的情况：
字符串读取完毕后，栈为空
